<h1>All Projects </h1>

Amount of projects: <%= @projects.count %>
<ul>
  <% @projects.each do |project| %>
    <li> <%= project.name %> </br>
          <%= project.summary %>
    </li>
  <% end %>
</ul>
